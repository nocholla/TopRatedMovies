<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nocholla.top.rated.movies">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tmdbmovies"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tmdbmovies"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash Screen -->
        <activity
            android:name=".features.splash_screen.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity android:name=".MainActivity" android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".features.about_us.AboutUsActivity" android:label="@string/menu_about_us"></activity>
        <activity android:name=".features.help.HelpActivity" android:label="@string/menu_help"></activity>
        <activity android:name=".features.grid_view.GridViewActivity" android:label="@string/menu_top_rated"></activity>
        <activity android:name=".features.list_view.ListViewActivity" android:label="@string/menu_top_rated"></activity>
        <activity android:name=".MovieDetailActivity" android:label="@string/menu_movie_detail" ></activity>

    </application>

</manifest>